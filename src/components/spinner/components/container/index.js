import { styled } from 'styletron-react'
import { spacing } from '../../../../common/style'

export default styled('div', {
  width: '100%',
  textAlign: 'center',
  margin: 0,
  padding: spacing.sd
})
