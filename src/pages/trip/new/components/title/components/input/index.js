import { styled } from 'styletron-react'
import { Input } from '../../../../../../../components/form'
import { fontFamily, fontSize, color } from '../../../../../../../common/style'

export default styled(Input, {
  fontFamily: fontFamily.heading,
  fontSize: fontSize.title,
  border: 0
})
