import { styled } from 'styletron-react'
import { spacing } from '../../../../../../../../../common/style'

export default styled('div', {
  display: 'flex',
  alignItems: 'center',
  padding: spacing.sm
})
